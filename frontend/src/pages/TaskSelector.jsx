/*
import React from 'react';

function TaskSelector({ onSelect }) {
  return (
    <div className="App">
      <h2>Select Task</h2>
      <button onClick={() => onSelect('dot_count')}>Dots</button>
      <button onClick={() => onSelect('mst')} style={{ marginLeft: '1em' }}>MST</button>
    </div>
  );
}

export default TaskSelector; */
